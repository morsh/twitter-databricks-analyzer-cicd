language: python
python:
  - "2.7"
virtualenv:
  system_site_packages: true
sudo: enabled
addons:
  apt:
    update: true
before-install:
  - sudo apt-get update
  - sudo apt-get install azure-cli
install:
  - make requirements
# command to run tests
script:
  - sudo ./deploy/travis/test.sh