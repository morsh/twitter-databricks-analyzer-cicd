language: python
python:
  - "2.7"
virtualenv:
  system_site_packages: true
sudo: enabled
addons:
  apt:
    update: true
install:
  - sudo apt-get update
  - sudo ./deploy/travis/install.sh
# command to run tests
script:
  - sudo ./deploy/travis/test.sh